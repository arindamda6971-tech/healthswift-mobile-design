project_id = "dmfqfswgnfpqeseqswhl"

[functions.analyze-prescription]
verify_jwt = true
# Runtime still performs a token validation check against Supabase Auth for extra safety

[functions.create-order]
verify_jwt = false
# JWT validation done in code via getClaims()

[functions.purchase-subscription]
verify_jwt = false
# JWT validation done in code via getClaims()

[functions.partner-hub-webhook]
verify_jwt = false
# Called by database webhook trigger with service role key

[functions.bridge-proxy]
verify_jwt = false
# JWT validation done in code via getClaims()

